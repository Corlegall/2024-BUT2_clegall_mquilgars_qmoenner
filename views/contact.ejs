<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="public/CSS/style.contact.css">
    <title>Location de Matériel</title>
</head>

<body>
    <nav class="navbar">
        <div class="logo">
            <img src="img/Logo_locafit.png" alt="Logo" class="logo-image">
        </div>
        <ul class="nav-links">
            <li><a href="index.html">Accueil</a></li>
            <li><a href="produits.html">Nos Produits</a></li>
            <li><a href="locations.html">Vos Locations</a></li>
        </ul>
        <div class="account">
            <a href="compte.html">
                <img src="img/profil.png" alt="Compte Client" class="account-icon">Cathy LePrince
            </a>
        </div>

    </nav>



    <div class="container">
        <section class="product">
            <div class="product-details">
                <div class="title-container">
                    <h2>CONTACT</h2>
                </div>
                <div class="info-section">
                    <p class="title">Email</p>
                    <p class="descr">LocaFit@gmail.com</p>
                </div>

                <div class="info-section">
                    <p class="title">Numéro</p>
                    <p class="descr">00.00.00.00.00</p>
                </div>

                <div class="info-section">
                    <p class="title">Createur site</p>
                    <p class="descr">Quentin Moenner - Maël Quilgars - Corentin LE GALL</p>
                </div>

                <div class="info-section">
                    <p class="title">Nos CV</p>
                    <p class="descr">00.00.00.00.00</p>
                    <button class="rent-button">Quentin Moenner</button>
                    <button class="rent-button">Maël Quilgars</button>
                    <button class="rent-button">Corentin LE GALL</button>
                </div>

            </div>
        </section>

    </div>
    <footer>
        <p>&copy; Contact & Condition d’utilisation
            2024 LocaFit - Tous droits réservés</p>
    </footer>
</body>

</html>

<style>
    body {
        background-color: #FFF3D4;
        /* Couleur de fond pour le corps */
        margin: 0;
        /* Supprime la marge par défaut */
        min-height: 100vh;
        /* Prend toute la hauteur de la fenêtre */
        display: flex;
        flex-direction: column;
        /* Permet au footer de se placer en bas */
    }

    .container {
        flex: 1;
        /* Permet à la zone de contenu de prendre tout l'espace restant */
        margin-top: 5%;
    }

    .navbar {
        display: flex;
        justify-content: space-between;
        /* Change pour distribuer l'espace */
        align-items: center;
        background-color: #A8D5BA;
        /* Couleur de fond */
        padding: 10px 20px;
        /* Espacement intérieur */
    }

    .logo {
        margin-right: 100px;
        /* Espace entre le logo et les liens (augmenté) */
    }

    .logo-image {
        max-width: 100px;
        /* Taille maximale du logo */
    }

    .nav-links {
        list-style: none;
        /* Supprime les puces de la liste */
        display: flex;
        /* Aligne les éléments de la liste en ligne */
        gap: 130px;
        /* Espacement entre les liens */
        flex-grow: 1;
        /* Permet à la liste de prendre tout l'espace disponible */
        justify-content: center;
        /* Centre les liens horizontalement */
        font-size: 25px;
        /* Ajustez la taille de police ici */
    }

    .nav-links a {
        display: flex;
        /* Utilise flex pour centrer le texte */
        align-items: center;
        /* Centre verticalement le texte */
        color: white;
        /* Change la couleur du texte */
        text-decoration: none;
        /* Supprime le soulignement */
        padding: 10px 15px;
        /* Ajoute de l'espacement autour du lien */
    }

    .account a {
        display: flex;
        /* Utilise flex pour centrer le texte */
        align-items: center;
        /* Centre verticalement le texte */
        color: white;
        /* Change la couleur du texte pour qu'elle soit visible */
    }


    .account {
        display: flex;
        /* Permet d'aligner l'image et le texte */
        align-items: center;
        /* Centre verticalement l'image et le texte */
        margin-left: 50px;
        /* Espace entre les liens et le compte client */
        font-size: 24px;
        /* Ajustez la taille de police ici */
    }

    .account a {
        display: flex;
        /* Utiliser flex pour centrer le texte par rapport à l'image */
        align-items: center;
        /* Centre verticalement le texte avec l'image */
        text-decoration: none;
        /* Supprime le soulignement du lien */
        color: white;
        /* Change la couleur du texte pour qu'elle soit visible */
    }


    .account-icon {
        max-width: 80px;
        /* Taille de l'icône (augmentée) */
        margin-right: 10px;
        /* Espace entre l'icône et le texte (augmenté) */
    }



    .product {
        display: flex;
        align-items: flex-start;
    }

    .info-section {
        margin-top: 30px;
        background-color: #f0f0f0;
        padding: 15px;
        border-radius: 10px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    .info-section p.title {
        font-size: 20px;
        font-weight: bold;
        margin-bottom: 10px;
    }

    .info-section p.descr {
        font-size: 18px;
    }

    .product-details {
        flex-direction: column;
        padding: 15px;
        background-color: rgba(240, 240, 240, 0.8);
        border-radius: 10px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
        margin-top: 20px;
        width: 80%;
        /* Ajuste la largeur à 80% de la section parent */
        max-width: 600px;
        /* Limite la largeur maximale si nécessaire */
        margin-left: 33%;
    }


    .title-container {
        display: flex;
        justify-content: center;
        /* Centre le titre dans le conteneur */
        width: auto;
        /* Prend toute la largeur disponible */
        margin-bottom: 10px;
        /* Espace en bas pour séparer du prix */
        background-color: rgba(168, 213, 186, 0.5);
        /* Couleur avec transparence */
        border-radius: 10px;
        /* Arrondi des coins */
    }


    .product-details>h2 {
        background-color: #A8D5BA;
        /* Couleur  */
        border-radius: 10px;
        /* Arrondi des coins */
        padding: 10px 15px;
        /* Espacement intérieur */
        margin: 0 0 10px 0;
        /* Marge en bas pour séparer du prix */
        color: #333;
        /* Couleur du texte */
        display: inline-block;
        /* Permet à l'élément de s'ajuster à son contenu */
        width: auto;
        /* Assure que la largeur s'ajuste au contenu */
    }


    .title {
        display: flex;
        justify-content: center;
        /* Centre le titre dans le conteneur */
        width: auto;
        /* Prend toute la largeur disponible */
        margin-bottom: 10px;
        /* Espace en bas pour séparer du prix */
        background-color: #f4c6c3;
        /* Couleur avec transparence */
        border-radius: 10px;
        /* Arrondi des coins */
    }

    .descr {
        display: flex;
        justify-content: center;
        font-size: 20px;
        /* Ajustez la taille de police ici */
        font-family: Arial, sans-serif;
    }


    .product-details p {
        line-height: 1.5;
        /* Espacement entre les lignes pour une meilleure lisibilité */
    }

    .thumbnail-gallery {
        display: flex;
        justify-content: center;
        flex-direction: column;
        flex-wrap: wrap;
        margin-top: 10px;
        /* Peut-être ajouter un peu plus de marge si nécessaire */

    }

    .image-gallery {
        align-items: center;
        margin-right: 20px;
        margin-left: 10%;
    }

    .nav-button {
        background-color: #3498db;
        color: white;
        border: none;
        padding: 10px;
        font-size: 1.5em;
        cursor: pointer;
        border-radius: 5px;
        transition: background-color 0.3s;
    }

    .nav-button:hover {
        background-color: #2980b9;
    }

    #mainImage {
        max-width: 500px;
        margin: 10px 0;
    }

    .thumbnail {
        max-width: 90px;
        height: auto;
        margin: 0 5px;
        cursor: pointer;
        border: 2px solid transparent;
        transition: border-color 0.3s;
    }

    .thumbnail:hover {
        border-color: #3498db;
    }

    .product-details {
        flex: 1;
        margin-top: 20px;
    }

    .rent-button {
        background-color: #f4c6c3;
        /* Couleur de fond */
        border: none;
        /* Supprime la bordure */
        border-radius: 25px;
        /* Arrondi des coins */
        padding: 10px 15px;
        /* Espacement intérieur */
        color: black;
        /* Couleur du texte */
        cursor: pointer;
        /* Change le curseur au survol */
        transition: background-color 0.3s;
        /* Ajoute une transition pour le survol */
        margin-top: 15px;
        /* Espace au-dessus du bouton */
        margin-left: 2%;
        font-size: 19px;
        /* Ajustez la taille de police ici */
    }

    .rent-button:hover {
        background-color: #e0b3b1;
        /* Change la couleur au survol */
    }


    footer {
        background-color: #41403F;
        /* Couleur de fond pour le footer */
        color: white;
        /* Couleur du texte */
        text-align: center;
        /* Centre le texte */
        padding: 15px 0;
        /* Espacement vertical */
        margin-top: 3%;
    }
</style>

</html>