<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Locations</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Inclure vos styles -->
</head>
<body>
    <%- include('header') %>

    <h1>Voici vos locations sur LocFit</h1>
    
    <div class="product-list">
        <% if (locations && locations.length > 0) { %>
            <% locations.forEach(location => { %>
            <div class="product-item">
                <img src="https://via.placeholder.com/200" alt="<%= location.produitMarque %> <%= location.produitModele %>">
                
                <div class="container_info">
                    <div class="product-title">
                        <%= location.produitMarque %> - <%= location.produitModele %>
                    </div>
                </div>

                <div class="product-location">
                    Loué par : <%= location.utilisateurPrenom %> <%= location.utilisateurNom %>
                </div>

                <div class="product-dates">
                  *
                    <% if (location.dateRetourEffective) { %>
                    <div class="product-date-effective"><strong>Date de retour effective : <%= location.dateRetourEffective %></strong></div>
                    <% } else { %>
                    <div class="product-date-effective"><strong>Retour non encore effectué</strong></div>
                    <% } %>
                </div>

                <div class="product-price">Prix total : <%= location.prix %> €</div>
                <div class="product-description">Veuillez respecter les délais de retour pour éviter les sanctions !</div>
            </div>
            <% }) %>
        <% } else { %>
            <p>Aucune location disponible.</p>
        <% } %>
    </div>
</body>
</html>




<style>

.container {
            display: flex;
            flex-wrap: wrap;

        }


        .product-item {
            flex: 1 1 calc(33.333% - 16px); /* Prend 1/3 de la largeur avec un petit ajustement pour l'espace */
            box-sizing: border-box;
            padding: 30px;
            
            border: 1px solid #ccc;
            text-align: center;
        }






body {

    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;
    background-color: #FFF3D4;
}


.presentation {

    
    margin-top: -10%;
    margin-bottom: 10%;
}

/* Conteneur pour la liste de produits */
.product-list {
    display: flex;
    flex-wrap: wrap;
    padding: 200px;
    gap: 20px;
    justify-content: center;
}

/* Cartes de produits */
.product-item {
    background-color: #A8D5BA;
    border-radius: 10px;
    width: 200px;
    padding: 15px;
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease, background-color 0.3s ease;
}

/* Style pour le titre du produit */
.product-title {
    font-size: 18px;
    margin-top: 10px;
    font-weight: bold;
    background-color: #A8D5BA;
    text-align: center;
}

/* Style pour le lieu de location */
.product-location {
    font-size: 14px;
    color: #333;
    margin-top: 10px;
    text-align: center;
    background-color: #FFF3D4;
}


/* Description du produit */
.product-description {
    font-size: 14px;
    color: #555;
    margin-top: 10px;
    padding: 3px;
    border-radius: 1px;

}

/* Effet au survol */
.product-item:hover {
    transform: scale(1.05);    
}


</style>

</html>